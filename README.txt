Blockchain Based Electoral System

- Demonstrates how we ca build a set of smart contracts to enforce integrity constraints required to maintain a secure votig system

You can use remix from ethereum to test the smart contracts. All you need is to compile the smart contracts and then run them from remix. 

Once this is done, you can also deploy the smart contracts to test networks supporting ethereum